---
title: Content属性Loading
date: 2023-07-12 17:00:00
---

import { Sandpack } from "@codesandbox/sandpack-react";
import { cobalt2, githubLight, aquaBlue } from "@codesandbox/sandpack-themes";

## Content 属性 Loading

- 利用 before 伪元素和 content 属性实现三个点 loading 动画
- content 的值可以是 Unicode, 使用 `\A` 即 LF 换行

<Sandpack 
  template="static"
  theme={aquaBlue}
  options={{
    showLineNumbers: true, // default - true
    showInlineErrors: true, // default - false
    wrapContent: true, // default - false
    editorHeight: 480, // default - 300
    editorWidthPercentage: 60, // default - 50
  }}
  files={{
    "/index.html": {
      code: 
        `<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta http-equiv="X-UA-Compatible" content="ie=edge" />
    <title>Content 属性 Loading</title>
    <style type="text/css">
      dot {
        display: inline-block;
        height: 1em;
        line-height: 1;
        vertical-align: -0.35em;
        text-align: left;
        overflow: hidden;
      }
      dot::before {
        display: block;
        content: '...\\A..\\A.';
        white-space: pre-wrap;
        animation: dot 3s infinite step-start both;
      }

      @keyframes dot {
        33% { transform: translateY(-2em); }
        66% { transform: translateY(-1em); }
      }

    </style>

  </head>
  <body>
    正在加载<dot>...</dot>
  </body>
</html>
`
      }

}}
/>
