---
title: 圆形进度条动画
date: 2024-01-02 22:30:00
---

import { Sandpack } from "@codesandbox/sandpack-react";
import { cobalt2, githubLight, aquaBlue } from "@codesandbox/sandpack-themes";

## 圆形进度条动画

<Sandpack 
  template="static"
  theme={aquaBlue}
  options={{
    showLineNumbers: true, // default - true
    showInlineErrors: true, // default - false
    wrapContent: true, // default - false
    editorHeight: 480, // default - 300
    editorWidthPercentage: 60, // default - 50
  }}
  files={{
    "/index.html": {
      code: 
        `<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <style type="text/css">
      body,
      html {
        width: 100%;
        height: 100%;
        display: flex;
        background: linear-gradient(45deg, #fff, #ffd);
      }

      @property --per {
        syntax: "<percentage>";
        inherits: false;
        initial-value: 0%;
      }

      @keyframes p2 {
        0% {
          --per: 100%;
        }
        100% {
          --per: 0%;
        }
      }

      .box {
        display: flex;
        align-items: center;
        justify-content: center;
        width: 160px;
        height: 160px;
      }
      .g-progress {
        position: absolute;
        margin: auto;
        width: 160px;
        height: 160px;
        border-radius: 50%;
        animation: p2 3s linear 1;
        background: conic-gradient(#f00 0, #f00 var(--per), #000 0%, #000);
        mask: radial-gradient(transparent, transparent 50%, #000 51%, #000 0);
      }
      .content {
        font: 64px bold;
      }
    </style>

  </head>

  <body>
    <div class="box">
      <div class="g-progress"></div>
      <div class="content">X</div>
    </div>
  </body>
</html>
`
      }

}}
/>
